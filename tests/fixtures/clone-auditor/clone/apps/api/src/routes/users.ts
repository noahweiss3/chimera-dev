// TODO(chimera): implement CRUD handlers for the User entity.
// Stubbed route: still has the marker, so the auditor should flag it.

import { Router } from "express";
const router = Router();
export default router;
