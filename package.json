{
  "name": "chimera-dev",
  "version": "0.1.0",
  "private": true,
  "description": "Reverse-engineer web applications into fully reconstructed clones",
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
